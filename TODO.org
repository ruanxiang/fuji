* Future Tasks
** DONE Adjust progress buffer position
Closed on 2025-12-26.
Optimize the window layout (UI/UX) so the progress buffer is less intrusive or more logically placed.

** TODO Test "Load Local Result" Mode
Verify that the workflow correctly handles existing Marker folders without re-ingesting or re-parsing.

** DONE Graphlit Integration
Closed on 2025-12-24.
- [X] Authentication fixed (JWT Secret)
- [X] Ingestion success recognition fixed
- [X] Context-aware chat implemented

** DONE Implement "Hybrid Context Mode"
Closed on 2026-01-05.
Shift from "Pure Proxy" to "Hybrid Argumentation".
1. Inject Marker/pdftotext markdown output directly into the *Nexus-Chat* buffer as system context.
2. Configure gptel to use standard backends (e.g., OpenAI, Anthropic) instead of solely delegating to Graphlit.
3. Retain Graphlit MCP as a tool for RAG/Long-term memory retrieval when context window is insufficient.

** DONE Implement Graphlit Content Management UI
Closed on 2026-01-05.
Essential for users on the free plan to manage quota.
1. valid implementation: Add  command.
2. Display a tabulated list of papers (Title, ID, Ingestion Date).
3. Allow marking items for deletion (bulk delete action via MCP ).

** TODO Digital Library Evolution: From Reader to Knowledge Archive
Transition the tool into a persistent, multi-format personal library with **plugin-based architecture**.

*** Architecture Overview
The system uses a **plugin-based design** with abstraction layers:
- **Extractor Plugins**: Modular text extraction (Marker, pdftotext, Pandoc, etc.)
- **RAG Backend Plugins**: Swappable knowledge retrieval (Graphlit, future alternatives)
- **Unified API**: `fuji--extract`, `fuji--rag-query`, etc.

See [[file:/home/ruan/.gemini/antigravity/brain/de9814b7-ce79-4551-9c77-0b78c5b2aba4/plugin_architecture.md][Plugin Architecture Design]] for details.

*** Implementation Phases

- [X] 0. Plugin Foundation (Abstraction Layer) **COMPLETE**
  - [X] Define unified Extractor API (`fuji--extract`)
  - [X] Define unified RAG Backend API:
    - [X] `fuji--rag-ingest` (upload text)
    - [X] `fuji--rag-query` (semantic search)
    - [X] `fuji--rag-list` (list all content)
    - [X] `fuji--rag-delete` (delete content)
    - [X] `fuji--rag-get-metadata` (retrieve metadata)
  - [X] Create plugin registry data structures
  - [X] Implement dispatcher logic (route calls based on file type and user config)
  - [X] Wrap existing code as plugins:
    - [X] Marker plugin
    - [X] pdftotext plugin
    - [X] Pandoc plugin
    - [X] Graphlit MCP plugin (implement all RAG API methods)
  - [X] **Refactor Library Manager** to use unified APIs:
    - [X] Replace `fuji--query-all-contents` with `fuji--rag-list`
    - [X] Replace `fuji--delete-from-graphlit` with `fuji--rag-delete`
    - [X] Ensure manager is RAG-backend agnostic
  - [X] **Runtime Plugin Switching (Hot-Swap)**:
    - [X] Implement `fuji-set-extractor` (switch PDF/DOCX extractor on-the-fly)
    - [X] Implement `fuji-set-rag-backend` (switch RAG backend on-the-fly)
    - [X] Use buffer-local/session variables to override defaults
    - [X] Dispatcher checks session override before config defaults
- [ ] 1. Two-Tier Configuration & Environment
  - [ ] Implement `fuji-configure` with **two-tier flow**:
    - [ ] **Tier 1: Tool Selection**
      - [ ] PDF Extractor:
        - [ ] pdftotext (required, always available as lightweight default)
        - [ ] Marker (optional, LLM-based high-quality extraction)
        - [ ] Offline pre-extracted results (user provides pre-processed Markdown)
      - [ ] DOCX/EPUB/HTML Extractor: Pandoc (required)
      - [ ] RAG Backend: Radio buttons (Graphlit [current], Local Vector DB [future])
    - [ ] ** DONE 1. Two-Tier Configuration & Environment
   CLOSED: [2026-01-09 Thu 15:17]

*** DONE Tier 1: Tool Selection UI
    CLOSED: [2026-01-09 Thu 15:17]
- [X] PDF Extractor selection (pdftotext/LLM-based)
- [X] LLM tool selection (marker/nougat/etc)
- [X] DOCX/EPUB/HTML Extractor (Pandoc)
- [X] RAG Backend selection (Graphlit/future alternatives)

*** DONE Tier 2: Tool-Specific Configuration
    CLOSED: [2026-01-09 Thu 15:17]
- [X] pdftotext binary path (auto-detect)
- [X] LLM tool binary path (marker/nougat)
- [X] Pandoc binary path (auto-detect)
- [X] Graphlit credentials (Org ID, Env ID, JWT Secret)
- [X] Cache and originals directories

*** DONE Smart Defaults & Auto-Detection
    CLOSED: [2026-01-09 Thu 15:17]
- [X] Auto-detect binary paths using executable-find
- [X] Sensible defaults for all configuration options
- [X] Configuration persistence via customize-save-variable

*** DONE Plugin Dispatcher Integration
    CLOSED: [2026-01-09 Thu 15:17]
- [X] Update fuji-extractor.el to use new configuration
- [X] Update fuji-rag.el to use new configuration
- [X] Ensure backward compatibility with legacy preferencesom `$PATH` (optional).
    - [ ] Pandoc Binary: Auto-detect using `executable-find`.
  - [ ] Define persistent variables (saved to custom file or `.emacs.d/`):
    - [ ] Plugin selections (which extractor/RAG backend for each file type)
    - [ ] Tool-specific configurations

- [X] 2. Storage & Archiving (Calibre-style)
  - [X] Implement automatic (optional) file archiving to `~/.cache/fuji/originals/`.
  - [X] Update metadata cache to track `original_path`, `archived_path`, and `doc_type`.
  - [X] Storage Strategy:
    - [X] Archive Copy: Copy the original file (PDF/DOCX) to `originals/` folder.
    - [X] Handle "Broken Links": 
      - [X] If original is missing, try opening the archived copy in `originals/`.
      - [X] If archived copy is also missing, fallback to opening the extracted text (Markdown) as read-only.
- [X] 3. Multi-Format Expansion (Pandoc Integration)
  - [X] Implement "Extractor" abstraction for DOCX, EPUB, and HTML using Pandoc.
  - [X] Add support for Web URLs (Web-to-PDF Strategy):
    - [X] Implement `fuji-web.el` to convert URLs to PDF using Headless Chrome.
    - [X] Update `fuji-configure.el` to detecting `google-chrome` or `chromium`.
    - [X] Unified Workflow: URL -> PDF (via Chrome) -> Markdown (via Marker/pdftotext) -> Graphlit.
- [X] 4. Library Manager & Persistent Sessions
  - [X] **Data Model Update**:
    - [X] Add `file_hash` (SHA-256) to metadata for duplicate detection.
    - [X] Add `memo` (User notes) and `title` (Editable display name) fields.
  - [X] **Direct File Open Mode** (`M-x fuji-read`):
    - [X] **Pre-check**: Calculate file hash -> Check Library.
    - [X] **If Existing**: Restore session (`fuji--resume-reading`) & Context.
    - [X] **If New**: Extract -> Ingest -> Init Session -> Save Metadata (w/ Hash).
  - [X] **Session Persistence**:
    - [X] Auto-save chat buffers to `~/.cache/fuji/sessions/[id].org`.
    - [X] Auto-load history when resuming a session.
  - [X] **Library Manager UI** (`M-x fuji-manager`):
    - [X] Columns: `[Icon] [Title] [Memo] [Date] [Size]`.
    - [X] **Interactive Commands**:
      - [X] `RET`: Open file side-by-side with restored Chat Session.
      - [X] `+`: Add New File (call `fuji-read`).
      - [X] `e`: Edit Title.
      - [X] `m`: Edit Memo.
      - [X] `d`: Mark for deletion / `x`: Execute.
    - [X] Window Behavior: Return to Manager without verify prompts on exit.
  - [X] **Phase 4.3: Search Functionality**
    - [X] Finish search functionality refinements (AND logic).
    - [X] Implement `fuji-library-search` command (Keybinding: `s`).
    - [X] Metadata Search (`t:title`, `m:memo`, `type:pdf`).
    - [X] Content Search (Full Text):
      - [X] Prefer `rg` (ripgrep) for speed.
      - [X] Fallback to `grep` if `rg` is missing (warn user).
      - [X] Execution: Synchronous (Simplest implementation).
    - [X] UI: Filter current view based on search results.
  - [X] **Integrated Reading Experience**:
    - [X] **Refactor Progress UI**: Move log messages to Chat Buffer (Top-level Org block/Drawer).
    - [X] **Window Layout**: Simplify to 2-window layout (Document | Chat).
    - [X] UI Fix: Ensure cursor automatically moves to `point-max` in chat buffers.
  - [x] **Phase 4.4: Bug Fixes & Refactoring**:
    - [x] **Path Portability**: Refactor `archived_path` and `results_dir` to store paths relative to `fuji-cache-directory`.
    - [x] **Config UX**: Refactor `fuji-configure` to only ask for the *name* of the originals directory (forcing subdirectory of cache root).
  - [X] **Phase 4.5: Knowledge Base Chat (Multi-doc RAG)**:
    - [X] **Interactive Command**: `fuji-library-chat-with-group` (Key: `W`).
    - [X] **Context Strategy**:
      - [X] Small group (< 5 docs): Inject full markdown context + RAG.
      - [X] Large group (> 5 docs): RAG-only mode (System prompt instruction + Graphlit ID filtering).
    - [X] **MCP Update**: Allow `query_graphlit` to accept a list of content IDs for scoped search.
    - [X] **UI**: Standalone Chat Buffer (`*Fuji-Group-Chat*`) without specific document window.

- [ ] 5. Command & Namespace Refactoring
  - [ ] Rename package commands to better reflect the "Library" concept (e.g., `fuji-read`, `fuji-manager`).
  - [ ] Consolidate command names to be more intuitive for the new workflow.
- [ ] 6. (Optional/Ambitious) Bibliography & Research Notes
  - [ ] DOI support: Auto-fetch metadata from CrossRef/OpenAlex.
  - [ ] Basic `org-ref` compatibility for looking up papers in the library.


*** Vision & Configuration
- Vision: Allow right-click on image links (from Marker) to "Ask Vision Model".
- Config: Add  and other direct config wrappers to bypass UI menus.

** TODO Grand Vision: "Emacs Antigravity"
Transform Nexus from a passive "Reader" to an active "Maker".
- **Agentic Coding**: The system should be able to write code, refactor files, and manage projects based on natural language instructions.
- **Autonomous Execution**: Similar to Google Antigravity, but native to the Emacs environment.
- Note: This is the long-term North Star for the project.
